# Parking lot

Here we keep the ideas for future development based on the feedback of helpers, attendees and trainers.

## TODO:
- Change the data to use only a single dataset through the training.
- Think about explaining df filtering old style df[rows, cols] -> df[df$row < 5, col]
- remove include=false from scopes
- add explanation of pipe starding with base form (i.e. filter(data, condition))
- Add the feedback form into the R-basic so that we dont forget to mention it.
- remove the seed() from exercises. It is confusing students
- add creating and using the project in R
- get rid of ISLANDER data and make the homework easily understandable dataset
- remove the case_when and instead explain ifelse (it is too much for a beginner to understandt the case_when syntax), ifelse is used more
- consider explaining left_join (since it is one of the most used functions in Data analysis)
- explain View and CMd+click shortcut
- update warmup file to add the ideas of functions. remove the lapply
- maybe explain Markdown concept


## Reminders
- Make sure to note that it is possible to run the whole code snippet by clicking the green "play" button. Fixes the problem of failing executions because earlier code lines have not been run.